<nav class="navbar navbar-expand bg-dark navbar-dark">
  <div class="container-fluid">  
    <ul class="navbar-nav nav-pills">
      <%= link_to "PostPost", root_path, class: 'navbar-brand', id: 'logo' %>
      <li class="nav-item">
        <%= link_to "首页", root_path, class: 'nav-link ' + yield(:root) %>
      </li>
      <li class="nav-item">
        <%= link_to "话题", topics_path, class: 'nav-link ' + yield(:topic) %>
      </li>
    </ul>
    <ul class="navbar-nav nav-pills d-flex" id='users-dropdown'>
      <% if logged_in? %>
        <li class="nav-item">
          <div class="dropdown">
            <button class="btn btn-primary dropdown-toggle" type="button" id="userMenu" data-bs-toggle="dropdown" aria-expanded="false">
              用户
            </button>
            <ul class="dropdown-menu dropdown-menu-right dropdown-menu-end" aria-labelledby="userMenu">
              <li><%= link_to "个人信息", current_user, class: 'dropdown-item' %></li>
              <li><%= link_to "修改信息", edit_user_path(current_user), class: 'dropdown-item' %></li>
              <li><%= link_to "注销", logout_path, method: :delete, class: 'dropdown-item' %></li>
            </ul>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "登录&注册", login_path, class: 'btn btn-primary'%>
        </li>
      <% end %>
    </ul>
  </div>
</nav>