<% provide(:title, @user.name) %>
<% provide(:dropdown, "active") %>

<div class="row">
  <aside class="col-md-2">
    <section class="user_info">
      <h1>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
  <div class="col-md-10">
    <%= render 'shared/feed' %>
  </div>
</div>